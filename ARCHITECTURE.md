# 🏗️ Billing System - Architecture & Data Flow

## 📐 System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    ELECTRON DESKTOP APP                          │
│                                                                  │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │                    MAIN PROCESS                            │ │
│  │                     (main.js)                              │ │
│  │                                                            │ │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │ │
│  │  │   Window     │  │  IPC Handler │  │   Database   │   │ │
│  │  │  Management  │  │  (Events)    │  │   Manager    │   │ │
│  │  └──────────────┘  └──────────────┘  └──────────────┘   │ │
│  │         │                  │                  │           │ │
│  └─────────┼──────────────────┼──────────────────┼───────────┘ │
│            │                  │                  │             │
│  ┌─────────┼──────────────────┼──────────────────┼───────────┐ │
│  │         │    PRELOAD.JS (IPC Bridge)          │           │ │
│  │         │    contextBridge API                 │           │ │
│  └─────────┼──────────────────┼──────────────────┼───────────┘ │
│            │                  │                  │             │
│  ┌─────────┴──────────────────┴──────────────────┴───────────┐ │
│  │              RENDERER PROCESS                              │ │
│  │                                                            │ │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │ │
│  │  │  index.html  │  │ renderer.js  │  │   main.css   │   │ │
│  │  │  (UI Layer)  │  │ (Logic)      │  │  (Styling)   │   │ │
│  │  └──────────────┘  └──────────────┘  └──────────────┘   │ │
│  │         │                  │                  │           │ │
│  │  ┌──────▼──────────────────▼──────────────────▼────────┐ │ │
│  │  │        User Interface Components                    │ │ │
│  │  │  • Login  • Dashboard  • Billing                    │ │ │
│  │  │  • Customers  • Products  • Reports                 │ │ │
│  │  └─────────────────────────────────────────────────────┘ │ │
│  └───────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
        ┌───────────────────────────────────────┐
        │         EXTERNAL SERVICES             │
        │  ┌─────────────┐  ┌──────────────┐   │
        │  │  SQLite DB  │  │ PDF Generator│   │
        │  │ (billing.db)│  │  (PDFKit)    │   │
        │  └─────────────┘  └──────────────┘   │
        └───────────────────────────────────────┘
```

---

## 🔄 Data Flow Diagrams

### 1️⃣ User Login Flow

```
┌──────────┐
│   User   │
└────┬─────┘
     │ 1. Enter credentials
     ▼
┌──────────────────┐
│  Login Form      │
│  (index.html)    │
└────┬─────────────┘
     │ 2. Submit event
     ▼
┌──────────────────┐
│  renderer.js     │
│  handleLogin()   │
└────┬─────────────┘
     │ 3. IPC Invoke: 'user:login'
     ▼
┌──────────────────┐
│  preload.js      │
│  (IPC Bridge)    │
└────┬─────────────┘
     │ 4. Forward to main
     ▼
┌──────────────────┐
│  main.js         │
│  IPC Handler     │
└────┬─────────────┘
     │ 5. Call DB method
     ▼
┌──────────────────┐
│  dbManager.js    │
│  authenticate()  │
└────┬─────────────┘
     │ 6. Query SQLite
     ▼
┌──────────────────┐
│  billing.db      │
│  users table     │
└────┬─────────────┘
     │ 7. Return user data
     ▼
┌──────────────────┐
│  main.js         │
│  Send response   │
└────┬─────────────┘
     │ 8. Return result
     ▼
┌──────────────────┐
│  renderer.js     │
│  Show dashboard  │
└────┬─────────────┘
     │ 9. Navigate
     ▼
┌──────────────────┐
│  Dashboard Page  │
└──────────────────┘
```

---

### 2️⃣ Create Bill Flow

```
┌──────────┐
│  Cashier │
└────┬─────┘
     │ 1. Select customer & products
     ▼
┌─────────────────────────────────────┐
│       Billing Page UI               │
│  • Customer dropdown                │
│  • Product selection                │
│  • Quantity input                   │
│  • Bill items table                 │
└────┬────────────────────────────────┘
     │ 2. Click "Add Product"
     ▼
┌──────────────────┐
│  renderer.js     │
│  addToBill()     │ ◄────┐
└────┬─────────────┘       │
     │ 3. Update UI        │ 4. Repeat
     ▼                     │    for more
┌──────────────────┐       │    products
│  Bill Items      │       │
│  Array           │ ──────┘
└────┬─────────────┘
     │ 5. Calculate totals
     ▼
┌──────────────────┐
│  Bill Summary    │
│  • Subtotal      │
│  • Tax           │
│  • Total         │
└────┬─────────────┘
     │ 6. Click "Generate Bill"
     ▼
┌──────────────────┐
│  renderer.js     │
│  generateBill()  │
└────┬─────────────┘
     │ 7. IPC: 'bills:create'
     ▼
┌──────────────────┐
│  main.js         │
│  Bill Handler    │
└────┬─────────────┘
     │ 8. Transaction start
     ▼
┌──────────────────────────────────────┐
│         dbManager.js                 │
│  ┌────────────────────────────┐     │
│  │ 1. Insert bill record      │     │
│  └────────┬───────────────────┘     │
│           ▼                          │
│  ┌────────────────────────────┐     │
│  │ 2. Insert bill items       │     │
│  └────────┬───────────────────┘     │
│           ▼                          │
│  ┌────────────────────────────┐     │
│  │ 3. Update product stock    │     │
│  └────────┬───────────────────┘     │
│           ▼                          │
│  ┌────────────────────────────┐     │
│  │ 4. Commit transaction      │     │
│  └────────────────────────────┘     │
└────┬─────────────────────────────────┘
     │ 9. Return bill ID
     ▼
┌──────────────────┐
│  main.js         │
│  Generate PDF    │
└────┬─────────────┘
     │ 10. Call invoice generator
     ▼
┌─────────────────────────────────────┐
│    invoiceGenerator.js              │
│  ┌────────────────────────────┐    │
│  │ 1. Create PDF document     │    │
│  └────────┬───────────────────┘    │
│           ▼                         │
│  ┌────────────────────────────┐    │
│  │ 2. Add header (logo, info) │    │
│  └────────┬───────────────────┘    │
│           ▼                         │
│  ┌────────────────────────────┐    │
│  │ 3. Add customer details    │    │
│  └────────┬───────────────────┘    │
│           ▼                         │
│  ┌────────────────────────────┐    │
│  │ 4. Add items table         │    │
│  └────────┬───────────────────┘    │
│           ▼                         │
│  ┌────────────────────────────┐    │
│  │ 5. Add totals & footer     │    │
│  └────────┬───────────────────┘    │
│           ▼                         │
│  ┌────────────────────────────┐    │
│  │ 6. Save PDF file           │    │
│  └────────────────────────────┘    │
└────┬────────────────────────────────┘
     │ 11. Return PDF path
     ▼
┌──────────────────┐
│  main.js         │
│  Open PDF        │
└────┬─────────────┘
     │ 12. shell.openPath()
     ▼
┌──────────────────┐
│  PDF Viewer      │
│  (OS Default)    │
└──────────────────┘
```

---

### 3️⃣ Dashboard Stats Flow

```
┌──────────┐
│   User   │
└────┬─────┘
     │ 1. Click "Dashboard"
     ▼
┌─────────────────────────────────┐
│  renderer.js                    │
│  loadDashboard()                │
└────┬────────────────────────────┘
     │ 2. IPC: 'dashboard:getStats'
     ▼
┌─────────────────────────────────┐
│  main.js → dbManager.js         │
│  getDashboardStats()            │
└────┬────────────────────────────┘
     │ 3. Execute parallel queries
     ▼
┌────────────────────────────────────────────────┐
│              SQLite Database                   │
│                                                │
│  Query 1: Today's sales ───┐                  │
│  Query 2: Today's bills ───┤                  │
│  Query 3: Total customers ─┤► Aggregate       │
│  Query 4: Total products ──┤                  │
│  Query 5: 7-day sales data─┘                  │
└────┬───────────────────────────────────────────┘
     │ 4. Return aggregated data
     ▼
┌─────────────────────────────────┐
│  renderer.js                    │
│  updateDashboardUI()            │
└────┬────────────────────────────┘
     │ 5. Update components
     ▼
┌────────────────────────────────────────────┐
│         Dashboard UI Elements              │
│  ┌──────────────┐  ┌──────────────┐       │
│  │ Today Sales  │  │ Today Bills  │       │
│  │   Card       │  │    Card      │       │
│  └──────────────┘  └──────────────┘       │
│  ┌──────────────┐  ┌──────────────┐       │
│  │  Customers   │  │  Products    │       │
│  │    Card      │  │    Card      │       │
│  └──────────────┘  └──────────────┘       │
│  ┌──────────────────────────────────┐     │
│  │     Sales Chart (Chart.js)       │     │
│  │     7-day trend visualization    │     │
│  └──────────────────────────────────┘     │
│  ┌──────────────────────────────────┐     │
│  │     Recent Bills List            │     │
│  └──────────────────────────────────┘     │
└────────────────────────────────────────────┘
```

---

## 🗄️ Database Schema Relationships

```
┌──────────────────────┐
│       users          │
│──────────────────────│
│ • id (PK)            │
│ • username           │
│ • password (hashed)  │
│ • role               │
│ • created_at         │
└──────────────────────┘
          (Used for authentication)


┌──────────────────────┐         ┌──────────────────────┐
│     customers        │         │      products        │
│──────────────────────│         │──────────────────────│
│ • id (PK)            │         │ • id (PK)            │
│ • name               │         │ • name               │
│ • phone              │         │ • price              │
│ • email              │         │ • tax                │
│ • address            │         │ • stock              │
│ • created_at         │         │ • created_at         │
└──────┬───────────────┘         └───────┬──────────────┘
       │                                 │
       │ 1:N                             │ 1:N
       │                                 │
       ▼                                 │
┌──────────────────────┐                 │
│       bills          │                 │
│──────────────────────│                 │
│ • id (PK)            │                 │
│ • customer_id (FK) ──┘                 │
│ • date               │                 │
│ • subtotal           │                 │
│ • tax                │                 │
│ • total              │                 │
│ • payment_mode       │                 │
└──────┬───────────────┘                 │
       │ 1:N                             │
       ▼                                 │
┌──────────────────────┐                 │
│    bill_items        │                 │
│──────────────────────│                 │
│ • id (PK)            │                 │
│ • bill_id (FK) ──────┘                 │
│ • product_id (FK) ─────────────────────┘
│ • product_name       │  (Denormalized)
│ • quantity           │
│ • price              │  (Snapshot)
│ • tax                │  (At time of sale)
│ • total              │
└──────────────────────┘

Legend:
PK = Primary Key
FK = Foreign Key
1:N = One-to-Many Relationship
```

---

## 🔐 Security Architecture

```
┌─────────────────────────────────────────────┐
│         Security Layers                     │
│                                             │
│  Layer 1: Context Isolation                │
│  ┌───────────────────────────────────┐     │
│  │ Renderer process cannot access    │     │
│  │ Node.js or Electron APIs directly │     │
│  └───────────────────────────────────┘     │
│                                             │
│  Layer 2: Preload Script Bridge            │
│  ┌───────────────────────────────────┐     │
│  │ Only whitelisted APIs exposed     │     │
│  │ via contextBridge                 │     │
│  └───────────────────────────────────┘     │
│                                             │
│  Layer 3: IPC Validation                   │
│  ┌───────────────────────────────────┐     │
│  │ Main process validates all        │     │
│  │ incoming IPC messages             │     │
│  └───────────────────────────────────┘     │
│                                             │
│  Layer 4: Database Security                │
│  ┌───────────────────────────────────┐     │
│  │ • Password hashing (SHA-256)      │     │
│  │ • Prepared statements (SQL inject)│     │
│  │ • Transaction isolation           │     │
│  └───────────────────────────────────┘     │
│                                             │
│  Layer 5: File System                      │
│  ┌───────────────────────────────────┐     │
│  │ • App data in user directory      │     │
│  │ • OS-level permissions            │     │
│  │ • No external file access         │     │
│  └───────────────────────────────────┘     │
└─────────────────────────────────────────────┘
```

---

## 📦 Module Dependencies

```
main.js
 ├─ electron (app, BrowserWindow, ipcMain, dialog)
 ├─ path
 ├─ fs
 ├─ dbManager.js
 │   └─ better-sqlite3
 └─ invoiceGenerator.js
     ├─ pdfkit
     ├─ fs
     └─ path

preload.js
 └─ electron (contextBridge, ipcRenderer)

renderer.js
 ├─ electronAPI (from preload)
 └─ chart.js

index.html
 ├─ main.css
 ├─ renderer.js
 ├─ chart.js (CDN)
 └─ font-awesome (CDN)
```

---

## 🔄 State Management

```
┌────────────────────────────────────────────┐
│         Application State                  │
│                                            │
│  Main Process State:                       │
│  ├─ db (DatabaseManager instance)         │
│  ├─ invoiceGen (InvoiceGenerator)         │
│  └─ mainWindow (BrowserWindow)            │
│                                            │
│  Renderer Process State:                   │
│  ├─ currentUser (logged-in user)          │
│  ├─ billItems[] (current bill)            │
│  ├─ allCustomers[] (cached)               │
│  ├─ allProducts[] (cached)                │
│  └─ salesChart (Chart.js instance)        │
│                                            │
│  Persistent State (SQLite):                │
│  ├─ users                                  │
│  ├─ customers                              │
│  ├─ products                               │
│  ├─ bills                                  │
│  └─ bill_items                             │
└────────────────────────────────────────────┘

State Updates:
1. UI action → renderer.js
2. IPC call → main.js
3. Database operation → SQLite
4. Response → renderer.js
5. UI update
```

---

## 🚀 Build & Deployment Flow

```
┌──────────────────────┐
│  Source Code         │
│  (Development)       │
└──────┬───────────────┘
       │
       │ npm run build-win
       ▼
┌──────────────────────┐
│  Electron Builder    │
│  ┌────────────────┐  │
│  │ 1. Package app │  │
│  └────┬───────────┘  │
│       ▼              │
│  ┌────────────────┐  │
│  │ 2. Copy files  │  │
│  └────┬───────────┘  │
│       ▼              │
│  ┌────────────────┐  │
│  │ 3. Bundle deps │  │
│  └────┬───────────┘  │
│       ▼              │
│  ┌────────────────┐  │
│  │ 4. Create NSIS │  │
│  │    installer   │  │
│  └────────────────┘  │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│  dist/               │
│  └─ BillingSystem-   │
│     Setup-1.0.0.exe  │
└──────┬───────────────┘
       │
       │ Run installer
       ▼
┌──────────────────────┐
│  Installed App       │
│  (Production)        │
│  C:\Program Files\   │
│     BillingSystem\   │
└──────────────────────┘
```

---

## 📊 Performance Considerations

```
┌──────────────────────────────────────────┐
│         Performance Optimizations        │
│                                          │
│  Database:                               │
│  ✓ SQLite WAL mode (better concurrency) │
│  ✓ Indexed columns (date, customer_id)  │
│  ✓ Prepared statements (query cache)    │
│  ✓ Transactions for bulk operations     │
│                                          │
│  UI:                                     │
│  ✓ Lazy loading of pages                │
│  ✓ Debounced search inputs              │
│  ✓ Virtual scrolling (future)           │
│  ✓ Cached data in renderer              │
│                                          │
│  IPC:                                    │
│  ✓ Async operations (non-blocking)      │
│  ✓ Batched requests when possible       │
│  ✓ Error handling with fallbacks        │
│                                          │
│  PDF:                                    │
│  ✓ Generated async                       │
│  ✓ Background process                    │
│  ✓ Separate thread                       │
└──────────────────────────────────────────┘
```

---

## 🔮 Future Architecture Enhancements

```
Current:
┌──────────────────┐
│  Desktop App     │
│  (Standalone)    │
│  └─ SQLite       │
└──────────────────┘

Future v2.0:
┌──────────────────┐       ┌──────────────────┐
│  Desktop App     │◄─────►│  Cloud Backend   │
│  └─ Local Cache  │  Sync │  (Firebase/AWS)  │
└──────────────────┘       └──────────────────┘
                                    │
                           ┌────────┴────────┐
                           │                 │
                   ┌───────▼──────┐  ┌──────▼───────┐
                   │  Mobile App  │  │   Web App    │
                   └──────────────┘  └──────────────┘
```

---

**Ready to build? 🚀 Check QUICK_START_GUIDE.md for usage instructions!**
